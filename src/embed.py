# embed.py
#   compute semantical word embeddings on wiki data
# by: Noah Syrkis

# imports
from dataset import Dataset
import torch
from torch import nn

# embedding
class Embed(nn.Module):
    def __init__(self):
          

           
